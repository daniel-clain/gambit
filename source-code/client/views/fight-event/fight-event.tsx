import * as React from 'react';
import { UIViewProps } from '../view';

export interface FightEventProps extends UIViewProps {
}

export default class C_FightEvent extends React.Component<FightEventProps> {
  public render() {
    return (
      <div>
        
      </div>
    );
  }
}
