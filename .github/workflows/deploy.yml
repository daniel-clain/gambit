name: Deploy Frontend and Backend to Vultr

on:
  push:
    branches:
      - fight-update-rework # or the branch you want to deploy from (e.g., 'master', 'release')

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20.11.1"

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add server to known_hosts
        run: ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: Install dependencies
        run: |
          yarn install --frozen-lockfile

      - name: Build the server
        run: |
          echo "Building server..."
          tsc -p tsconfig.server.json  # Build server with TypeScript
          echo "Build complete. Listing files:"
          ls -l ./server-build-prod  # List the files in the build directory

      - name: Wait for server build directory to exist
        run: |
          until [ -d "./server-build-prod" ]; do
            echo "Waiting for server build to complete..."
            sleep 5
          done

      - name: Log working directory and files after build
        run: |
          echo "Current working directory: $(pwd)"  # This will log the current directory
          ls -l  # List files in the current directory to confirm if 'server-build-prod' exists
          ls -l ./server-build-prod  # List files inside the 'server-build-prod' folder

      - name: Log working directory before SCP
        run: |
          echo "Current directory before SCP: $(pwd)"
          ls -l ./server-build-prod

      - name: Wait for build directory
        run: |
          echo "Waiting for build directory..."
          until [ -d "./server-build-prod" ]; do
            sleep 2
            echo "Still waiting for build directory..."
          done
          echo "Build directory found, proceeding with SCP"

      - name: Transfer backend build to server
        run: |
          scp -r ./server-build-prod/* ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:${{ secrets.SERVER_APP_DIR }}/server-build-prod

      - name: Build frontend
        run: |
          yarn build-prod

      - name: Transfer frontend build to server
        run: |
          scp -r host-packages/main-game-prod/* ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:${{ secrets.SERVER_APP_DIR }}/host-packages/main-game-prod/

      - name: Transfer package.json, yarn.lock, favicon.ico to server
        run: |
          scp package.json yarn.lock favicon.ico ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:${{ secrets.SERVER_APP_DIR }}

      - name: SSH into server and run deployment (Install dependencies and build)
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} "
            # Ensure NVM is loaded and available
            source ~/.bashrc  # or ~/.bash_profile if using a different shell

            # Use the correct Node.js version with NVM
            export NVM_DIR=\$HOME/.nvm
            [ -s \$NVM_DIR/nvm.sh ] && . \$NVM_DIR/nvm.sh
            nvm use 20.11.1

            # Verify yarn is installed and working
            yarn --version

            # Navigate to the app directory
            cd ${{ secrets.SERVER_APP_DIR }}

            # Install production dependencies
            yarn install --frozen-lockfile --production &
          "

      - name: Transfer SSL certificates to server
        run: |
          scp -r sslcert ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:${{ secrets.SERVER_APP_DIR }}/sslcert

      - name: SSH into server and reload the app
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} "
            # Ensure environment is loaded for pm2
            source ~/.bashrc  # or ~/.bash_profile if using a different shell
            
            # Verify pm2 is available
            pm2 -v

            # Start or reload the app with PM2
            pm2 startOrReload ecosystem.config.js --env production
            pm2 save
          "
